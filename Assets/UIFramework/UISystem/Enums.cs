namespace ShonEagle.Tools.UI
{
    public enum PopupName
    {
        None = 0,
        LoadingPopup,
    }

    public enum ScreenName
    {
        None = 0,
        MainMenu,
        GamePlay,
        LevelComplete,
        GameOver
    }
}